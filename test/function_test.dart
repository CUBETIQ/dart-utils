import 'package:test/test.dart';
import 'package:function_utils/extensions/string_validation.dart';

void main() {
  test('parsing number from string', () {
    var text = '';

    expect(true, equals(text.isBlank));
  });
}
